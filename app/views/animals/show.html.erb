<p id="notice"><%= notice %></p>
<div class="container">
  <div class="row">
    <div class="col-md-6">
      <img width="450px" src= "<%= @animal.image_url %>" />
    </div>     
    <div class="container">
      <div class="row">
    <div class="col-md-6">
      <h1><%=@animal.title %></h1>
      <h2>Description: </h2>
      <h3> <%= @animal.description %> </h3>
      <h2> Tags: </h2>
      <h3><%= @animal.tags %> </h3>
  <h4> <%= link_to 'Edit', edit_animal_path(@animal) %> |
    <%= link_to 'Back', animals_path %></h4>
    </div>
